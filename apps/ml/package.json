{
  "name": "merlin-ml",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bash -c 'source .venv/bin/activate && uvicorn app:app --host 0.0.0.0 --port 8000 --reload'",
    "test": "bash -c 'source .venv/bin/activate && pytest'"
  }
}
